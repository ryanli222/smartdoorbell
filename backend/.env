# Database
POSTGRES_USER=doorbell
POSTGRES_PASSWORD=doorbell_secret
POSTGRES_DB=doorbell

# MinIO
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio_secret
MINIO_BUCKET=doorbell-snapshots

# External MinIO endpoint (for presigned URLs accessible from outside Docker)
MINIO_EXTERNAL_ENDPOINT=localhost:9000
